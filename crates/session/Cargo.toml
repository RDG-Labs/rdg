[package]
name = "session"
version = "0.1.0"
edition.workspace = true
publish.workspace = true
license = "GPL-3.0-or-later"

[lints]
workspace = true

[lib]
path = "src/session.rs"
doctest = false

[features]
test-support = [
    "db/test-support",
]

# As `test-support`, but a production-faithful token that benchmark graphs can
# enable without pulling in the `test-support` feature or any of its doubles.
bench-support = []

[dependencies]
db.workspace = true
gpui.workspace = true
uuid.workspace = true
util.workspace = true
serde_json.workspace = true
