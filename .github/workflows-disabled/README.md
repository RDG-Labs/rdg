# Disabled GitHub workflows

These inherited workflows are retained for reference but are not active in the Rdg
repository. GitHub only loads workflow files directly from `.github/workflows`, so
these files are intentionally kept outside that directory while the Rdg release
workflow remains active.

To re-enable one, move it back to `.github/workflows` after reviewing its runners,
secrets, repository references, and permissions.
